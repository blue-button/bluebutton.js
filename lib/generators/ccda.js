/*
 * ccda.js
 */

var Generators = {};

Generators.CCDA = (function () {
  
  /*
   * Generates a CCDA document
   */
  var run = function (json) {
    var ccda = json;
    return ccda;
  };
  
  
  // Reveal public methods
  return {
    run: run
  };
  
})();
