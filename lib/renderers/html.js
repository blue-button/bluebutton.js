/*
 * ...
 */

Renderers.HTML = (function () {
  
  /*
   * Renders HTML from Blue Button JSON
   */
  var run = function (json) {
    var html = json;
    return html;
  };
  
  return {
    run: run
  };
  
})();
